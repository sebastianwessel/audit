TICKET-101:
  status: in_progress
  resume_notes: Root topology, safe output creation, non-mutating artifact reads, strict source-admission manifest, private immutable source/context snapshots, snapshot-backed workflow tools and resume, plan sealing, exact checkpoint identity, explicit empty-scope incompleteness, source-backed not-applicable provenance, a sole terminal reducer, fail-closed exclusive run lease, source-free audit attempt record, and report/manifest/checkpoint/grounding-draft version alignment are implemented. Snapshot retention uses a target-keyed, run/context-bound ownership index and removes bytes only after its final resumable owner releases them. Existing leases are never stolen; a known-abandoned lease needs operator resolution before explicit resume. Product CLI commands now validate a closed command-specific option set before configuration or I/O; fake-provider E2E coverage proves snapshot-backed resume after target mutation and preserves reached checkpoints on report-publication failure. Canonical evidence maps now make mapped and unanswered obligations mutually exclusive. Strict closure, vector-result, and report schemas conserve map/posture/finding counters and reject contradictory persisted queues or coverage before recovery, lineage, reporting, or evaluation can consume them. Candidate-aware verifier/countercheck dispatch now shares one run-wide pool with no candidate/evidence cap and per-candidate `pending`/`running`/redacted-`completed` recovery records. Reuse binds the complete workflow protocol plus exact candidate fingerprint; interrupted work is rescheduled without discarding completed work. Incomplete map/posture drafts are not checkpointed. Provider cancellation closes queued candidate-aware dispatch. Report v15 removes the misleading inferred attack-chain field and accepts only `accepted` confirmed findings or `needs-review` queue items; v14 artifacts are deliberately rejected.
  last_verified: bun run format && bun run schema:generate && bun run spec:check && bun run schema:check && bun run typecheck && bun run lint && bun test && bun run check && bun run test:coverage
  current_proof: 330 offline tests pass; line coverage is 85.99% and function coverage is 91.63%. Format, schema generation/check, spec check, typecheck, lint, deterministic fixture evaluation, deterministic corpus evaluation, full check, coverage gate, and evaluator-only acquired-source-pair validation pass without provider dispatch. The fixture evaluation remains intentionally non-qualifying and reports 0/11 expected findings because it does not invoke a real model.
  affected_specs:
    - specs/03-architecture/09-immutable-evidence-and-terminal-truth.md
