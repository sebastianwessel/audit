# Artifact store

Performs atomic, output-jail-aware JSON, Markdown, and feature-owned private UTF-8 writes. It owns containment and filesystem safety only; report projection remains in the audit-report feature, while source-admission owns snapshot content and validation.
