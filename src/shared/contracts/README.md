# Shared contracts

Strict Zod schemas for universal primitives such as ids, timestamps, normalized tokens, model-route identifiers, and common error envelopes. Feature-specific schemas stay with their feature.
