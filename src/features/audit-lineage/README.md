# Audit lineage

Owns the source-free, coverage-aware comparison of two validated audit reports. It uses the existing exact finding identity and never reads a target, invokes a provider, or makes a security conclusion from language-specific signals.
