# Target inventory

Owns target/context inventory, strict source-admission and snapshot-manifest contracts, immutable source snapshots, and their colocated tests. Target files are language-agnostic: a recognized hint is optional metadata, while unknown UTF-8 files remain inventory and review evidence. The default policy records named exclusions in the manifest; it never hides an admitted file because of its language, size, line count, or extension.
