# Features

A feature folder is the primary ownership boundary. Place schema, inferred types, pure rules, application orchestration, public exports, and unit tests together. Feature-to-feature imports must use the exporting feature boundary, never internal files.
