# Specification migrations

Record changes to persisted plan, finding, report, and run artifacts here. A migration note is required when schema meaning, requiredness, enum values, approval semantics, or path rules change.
