# Public coverage limitations

## Decision

`PublicAuditReportSchema` v4 retains the existing closed vector-coverage limitation codes. They make incomplete or constrained audit coverage understandable without publishing source, context, prompts, tool output, or model prose.

## Breaking change

Public report v3 is rejected. No reader, converter, or compatibility projection is retained.
