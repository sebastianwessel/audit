# Cross-cutting tests

Unit and schema contract tests live beside the code they verify as `*.test.ts` or `*.contract.test.ts`. This directory is reserved for fixtures, test support, integration, and end-to-end scenarios that intentionally span feature/platform boundaries.
