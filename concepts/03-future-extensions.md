# Future extensions

Possible later capabilities include diff-focused plans, SARIF output, deterministic matchers, a validation panel, provider ensembles, durable resumable runs, and a human review UI.

Each extension needs its own scope, threat model, contract, migration, and verification decision before implementation.
